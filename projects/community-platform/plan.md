# Community Platform — Implementation Plan

**Status:** Stub — follows spec approval
**Date:** 2026-05-01
**Spec:** [`spec.md`](spec.md) *(stub — pending brainstorm)*

---

## ⚠️ Plan is intentionally empty

This file becomes real **after** [`spec.md`](spec.md) is filled in and accepted by the founder. The flow is:

1. Brainstorm (`superpowers:brainstorming`) → produces draft spec.
2. Founder reviews + accepts spec.
3. Plan-writing (`superpowers:writing-plans`) → produces this file.
4. Execution (`superpowers:executing-plans` + `/tdd` + `/code-review`) → ships v0.1.

**Do not pre-write milestones here.** Premature milestones bias the brainstorm.

---

## Milestones

*(Empty — fill in via `superpowers:writing-plans` after spec is accepted.)*

## Dependencies

To be sized in plan-writing. Expected feed-ins:

- [`persona-builder/`](../../persona-builder/) — persona files.
- [`projects/gbrain/`](../gbrain/) — archive content + Q&A surface.
- [`community/members/roster.md`](../../community/members/roster.md) — current roster.

## Timeline

*(Empty.)*

## Test plan

Will follow [`docs/playbooks/ai-collaborator-stack.md`](../../docs/playbooks/ai-collaborator-stack.md) §"Per-feature workflow":

- Unit + integration tests via `/tdd` (80%+ coverage).
- E2E for critical role-based flows via `/e2e`.
- Code review via `/code-review` and `code-reviewer` agent before merge.
- Verification via `/verify` before tag.

## Rollout

*(Empty — fill in plan-writing.)*

## Rollback

*(Empty — fill in plan-writing.)*
