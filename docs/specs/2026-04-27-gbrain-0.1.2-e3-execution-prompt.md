# GBrain 0.1.2 — E3 execution prompt (Real-channel readiness + tag)

**Date:** 2026-04-27
**Branch:** `gbrain-0.1.2-ask-bundle` (already pushed; E3 continues here, then merges to `main` at the tag point)
**Plan:** [`projects/gbrain/plan-0.1.2-ask-bundle.md`](../../projects/gbrain/plan-0.1.2-ask-bundle.md) — Phases 12–16 (7 tasks)
**Driving spec:** [`docs/specs/2026-04-26-gbrain-ask-search-help-design.md`](./2026-04-26-gbrain-ask-search-help-design.md) v2.1
**ADRs:** [0008](../decisions/0008-file-based-embeddings-index.md) · [0009](../decisions/0009-prompt-modules-and-injection-guards.md) · [0010](../decisions/0010-summarize-deferred.md)
**Predecessor:** [E2 closeout](./2026-04-27-gbrain-0.1.2-e2-closeout.md)
**Successor:** none planned (E3 ends with `gbrain-v0.1.2` tag).

---

## How to use this file

Open a fresh Claude Code chat in this repo (`/Users/antonsafronov/Projects/Warsaw AI Comunity`).

Set effort: `/effort` → `max`.

Paste the section [`## Copy-paste prompt`](#copy-paste-prompt) below as the first message.

---

## Copy-paste prompt

Read these five files in order, then start executing the plan.

1. docs/specs/2026-04-27-gbrain-0.1.2-e2-closeout.md
   — E2 closeout. Tells you what's already shipped (Phases 0–11, 24 commits across
     E1+E2), the carried-forward documented adaptations, the quality-debt backlog
     (QD-1..QD-15) you'll address in a single E3 cleanup pass before the tag, and
     what's deliberately not in E2 (= what's in scope for E3).

2. projects/gbrain/plan-0.1.2-ask-bundle.md  (Phases 12–16 only)
   — The implementation plan. E3 covers 7 tasks across 5 phases:
     · Phase 12 — GitHub Action workflow (Task 12.1, has HUMAN PAT step)
     · Phase 13 — pinned-message ops script (Task 13.1, has HUMAN STAGING_CHAT_IDS step)
     · Phase 14 — production verification endpoint, time-boxed (Tasks 14.1, 14.2)
     · Phase 15 — calibration gate (Task 15.1, HUMAN: author query set + tune threshold)
     · Phase 16 — day-30 rehearsal + tag (Tasks 16.1, 16.2)
     Skip Phases 0–11 (already shipped in E1+E2). Each task has Files, a failing
     test (where applicable), a run command, a minimal implementation, a passing-
     test verification, and a commit message. Follow it task-by-task; do not jump
     phases.

3. docs/specs/2026-04-26-gbrain-ask-search-help-design.md  (selected sections)
   — The design spec (v2.1).
     · §3.6 covers the GitHub Action workflow file (relevant to Phase 12).
     · §3.7 covers deploy bundle delivery + the verification endpoint pattern
       (relevant to Phase 14).
     · §4.4 covers the pinned-message generator + chat-id allowlist (Phase 13).
     · §8.2 covers the in-line gates including the calibration gate (Phase 15).
     · §8.3 covers what this spec does and does not do (sanity-check before tag).
     · §1 (Goal + scope) and §8.1 (sequencing) inform the day-30 rehearsal gates
       (Phase 16).

4. docs/decisions/0008-file-based-embeddings-index.md
   — Skim. The Action workflow respects the `community/archive/_index/` write-only
     contract from this ADR. Phase 12's `assertAllowedPath` check + `git clean -fd`
     pre-commit guard fall out of this ADR.

5. projects/gbrain/app/scripts/build-index.ts
   — IMPORTANT: This file has a known latent CLI repoRoot resolution bug (QD-8 in
     the closeout). At lines 179–180, `path.resolve(new URL(import.meta.url).pathname,
     "..", "..", "..", "..")` lands at `projects/`, not repo root. The fix is one
     extra `..` (5 total) since `path.resolve` from a file path needs N+1 `..`s for
     N directory levels. **Phase 12 MUST fix this before the GitHub Action will work.**
     Suggested approach: change to `path.resolve(scriptDir, "..", "..", "..", "..")`
     where `scriptDir = path.dirname(fileURLToPath(import.meta.url))` (mirroring
     scripts/copy-index.ts's pattern).

After reading those, execute the plan starting at Phase 12 / Task 12.1, but FIRST
fix QD-8 in `scripts/build-index.ts` as a small prerequisite commit (separate from
Phase 12's commit). The Action calls the script as a CLI; the bug fires there.

Use superpowers:subagent-driven-development to drive task-by-task execution, EXCEPT
for the human-in-loop tasks (12.1 PAT creation, 13.1 STAGING_CHAT_IDS, 15.1 query
authoring + threshold tuning) which require Anton's input — the chat must pause
and ask. Re-dispatch the implementer once Anton supplies the input.

E3 entry conditions (verify before starting):

- Working directory: /Users/antonsafronov/Projects/Warsaw AI Comunity
- Branch: gbrain-0.1.2-ask-bundle (already exists; checkout it; do NOT switch to main)
- Working tree: clean (only .claude/ untracked, pre-existing)
- Tests: cd projects/gbrain/app && pnpm test  →  160 passed | 7 skipped (167)
- Typecheck: pnpm typecheck  →  clean

If any of these don't hold, stop and ask.

E3 PHASE FIREWALL CHANGES from E2 (read carefully):

E2 said "Don't touch real channel, don't rotate token, don't tag." For E3:

- TELEGRAM_BOT_TOKEN rotation IS in scope (Phase 16 day-30 gates) if the existing
  staging token is going to be reused for production. Anton decides at gate time.
- CHAT_ID and topic IDs ARE switched from staging to real-channel values during
  Phase 16. Anton's decision; this prompt does not pre-commit.
- 0.1.2 tag IS the gate-pass deliverable (Task 16.2). After all six day-30 gates
  pass on staging.
- The GitHub Action build-index.yml WILL run against the real archive once the
  PAT is wired (Phase 12). Until then, dry-run only.
- The /api/debug/index-presence endpoint is created (Phase 14.1) and removed
  (Phase 14.2) within E3. Time-boxed; do NOT leave it in the repo at tag time.
- The Vercel AI Gateway re-introduction is still NOT in scope (per 0.1.1 direct-
  Gemini decision). 0.3.0+ may revisit.

Constraints (carried forward + new):

- Do NOT introduce new ADRs without asking. ADR-0008/0009/0010 cover everything
  in scope. If a task surfaces a decision that warrants an ADR (e.g., calibration-
  threshold provenance), stop and ask.
- Do NOT expand scope to /summarize (ADR-0010 defers it).
- DO address the entire quality-debt backlog (QD-1..QD-15) in a single dedicated
  cleanup commit between Phase 15 (calibration) and Phase 16 (tag). Reasonable
  scope: ~30-50 lines net change across the listed files. Rationale: the cleanup
  closes the gap between "plan-faithful" and "production-ready", and we want
  0.1.2 tagged on a clean base.
- Do NOT skip the human-in-loop steps. Each one is gated for a reason:
  · 12.1 PAT — Anton must scope a fine-grained PAT to `community/archive/**`
    push permission only. Bot account `gbrain-index-bot` must be added as a
    repo collaborator.
  · 13.1 STAGING_CHAT_IDS — Anton must supply the literal numeric chat ids of
    the staging supergroup before the script will run.
  · 15.1 calibration — Anton must author the ≥10 known-positive + ≥10 known-
    negative `/ask` queries and run them against the staging /ask endpoint
    with various cosine thresholds, then commit the chosen threshold.
- Address each of the seven E1+E2 carried-forward documented adaptations
  faithfully:
  · ESM `__dirname` substitution → use `fileURLToPath(import.meta.url)` pattern.
  · LANDED 4-arg `BotClient.sendMessage(chatId, threadId, text, parseMode?)` —
    Phase 13's regen-pinned does NOT use BotClient (uses Telegram API directly
    for pinning + sending) so signature concern doesn't apply there. Phase 14's
    debug endpoint returns JSON, no Telegram.
  · `tests/safety/*.test.ts` suffix for Vitest 3 auto-discovery (carried).
  · Webhook dispatch above DM/supergroup split (carried; no new commands in E3).

Anton's preferences (from
/Users/antonsafronov/.claude/projects/-Users-antonsafronov-Projects-Warsaw-AI-Comunity/memory/):

- never echo/store API keys pasted in chat (feedback_secret_handling) — applies
  to PAT (Phase 12), TELEGRAM_BOT_TOKEN (Phase 16), GEMINI_API_KEY for live
  injection-case validation. Use env-var references, never paste literal values.
- explain impact + recommend on decisions, don't present neutral menus
  (feedback_decisions_with_recommendation).

Human-in-the-loop steps in E3 (the chat must pause for each):

1. Task 12.1 — Anton creates a fine-grained PAT for `gbrain-index-bot`, adds the
   token as `INDEX_BOT_TOKEN` repo secret. The implementer cannot proceed past
   this point until Anton confirms the token is wired.
2. Task 13.1 — Anton supplies staging supergroup chat ids (numeric) for the
   `STAGING_CHAT_IDS` constant. The implementer cannot complete the script
   without this.
3. Task 15.1 — Anton authors and runs the calibration query set on staging.
   The implementer can write the harness/script that runs the queries and tunes
   the threshold; Anton runs it and commits the chosen threshold value to
   `commands/ask.ts:ASK_SIMILARITY_THRESHOLD`.

Two prerequisite tasks E3 must do before Phase 12 (separately committed):

- Fix QD-8 (build-index.ts CLI repoRoot 4→5 `..` count). Standalone commit; one-
  line change.
- Optionally batch QD-1..QD-7+QD-9..QD-15 into one cleanup commit BEFORE Phase 16.
  See the closeout for details. This is recommended discipline ("tag on clean base")
  but not strictly required if individual phases would naturally surface them.

E3 expected outcome (the gate before tag):

- 7 task commits + 1 prerequisite commit + 1 cleanup commit on
  `gbrain-0.1.2-ask-bundle`.
- Full test suite green: ≥ ~165 passing (a few new tests for build-index
  workflow and calibration harness — exact count not load-bearing; "no failures"
  is). Live injection-cases must pass when run with `GEMINI_API_KEY` set, against
  the LANDED prompts/ask.ts.
- Typecheck clean.
- All six day-30 gates pass on staging:
  · 3 known-positive `/ask` queries return correctly cited answers.
  · 3 list-style `/search` queries return correct ranked results.
  · `/help` and `/help <command>` render as expected.
  · Pinned message regenerates correctly (Phase 13).
  · GitHub Action build-index runs in <60s and the resulting index round-trips
    through `/ask` (Phase 12 + 14 verifying).
  · Calibration threshold committed (Phase 15).
- `/api/debug/index-presence` endpoint added then removed within E3 (Phase 14.1
  → 14.2).
- CHANGELOG updated and `gbrain-v0.1.2` tagged at HEAD of the branch (Task 16.2).
- Branch fast-forward merges into `main` (Anton runs the merge; not in this
  chat's scope unless explicitly authorized).

Start by acknowledging the plan + entry conditions in your own words (one
paragraph: what E3 ships, what's now in scope vs E2, the three human-in-loop
gates, the QD-8 prerequisite fix, which task you'll do first), then move to
the QD-8 fix as a one-commit prerequisite, THEN Phase 12 / Task 12.1.

---

## Status of source file

Committed at `docs/specs/2026-04-27-gbrain-0.1.2-e3-execution-prompt.md` (commit hash will be the closeout+prompt commit added at end of E2). The fenced block above and this `## Copy-paste prompt` section are byte-identical content; you can also open the file in the new chat and copy from there if you prefer.

One sanity check before you start E3: verify the branch HEAD with
`git log -1 --oneline gbrain-0.1.2-ask-bundle` — it should be the E3 execution-
prompt commit (this file's commit). The fresh chat will run that check itself
per the entry-conditions block, but worth confirming locally too.
