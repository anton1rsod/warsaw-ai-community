# GBrain 0.1.1 — Session 4 Completion Plan

> **For agentic workers:** This is an operational runbook (not a code-feature plan). Tasks are bite-sized, ordered, and each lists exact API calls / files / verification. Tracked live via TaskCreate.

**Goal:** Rotate the 4 secrets exposed in session 3, finish rehearsal tests C4–C6, run Phase D pre-launch cleanup, do a code review of session 2-3 changes, brainstorm extensions, and ship as **0.1.1**.

**Audience:** Anton + the next Claude Code session if this one doesn't finish.

**Supersedes operationally:** [`2026-04-26-gbrain-rehearsal-session-3-closeout.md`](./2026-04-26-gbrain-rehearsal-session-3-closeout.md) §7 PRE-WORK + remaining tests + Phase D.

**Tech surface:** Vercel REST API · Telegram Bot API · git CLI · Next.js (already deployed at `warsawaicommunitygbrain.vercel.app`) · `code-reviewer` agent · `superpowers:brainstorming` skill.

---

## 0. Status snapshot at start of session 4

- Code: at sha `b78f454` (visible in `origin/main`); General-topic-fix is at `630c557` (force-pushed in session 3 — verify deployment alias).
- CHANGELOG: `[0.1.0] - 2026-04-24` (matches git tag `gbrain-v0.1.0`).
- `app/package.json`: `"version": "0.0.1"` (mismatched — task #3 fixes this).
- Vercel project: SSO disabled, env vars rotated, webhook registered.
- Telegram: 8 topics in `gbrain-staging`, bot has `can_manage_topics: true`.
- Rehearsal: C1 ✅ 3/3 · C2 ⚠️ partial · C3 ⏸ deferred · C4 ⏸ · C5 ⏸ · C6a ⏸ · C6b ✅ · C6c ⏸.
- Exposed secrets in chat history: `CRON_SECRET`, `TELEGRAM_WEBHOOK_SECRET`, Vercel API token (all rotated this session); `TELEGRAM_BOT_TOKEN` rotation needs Anton's `/revoke`.

## 1. Versioning model (decided 2026-04-26)

```
0.1.0  Phase 1 scaffold — feature complete, never run on real channel  (DONE)
0.1.1  Rehearsal complete + Phase D cleanup                            (THIS SESSION)
0.1.x  Subsequent rehearsal/staging fixes if needed
0.2.0  First real-channel soft launch (Phase E, separate chat)
0.2.x  Real-channel patches, observation-window fixes
0.3.0  Phase 1 success gates met (≥5 archivers, ≥10 items, 14d digest)
0.4.0  Phase 2 — Q&A with RAG, basic
0.5.0+ Phase 2 mature, Phase 3 features
1.0.0  Reserved — Anton's call when GBrain is ready to be claimed production-grade
```

**Rule going forward:** Stay in `0.x` indefinitely. Don't auto-promote to 1.0.0 on hitting a milestone. Every session ends with a CHANGELOG entry under the next patch (or minor) version. Tag at session-close. No silent version drift.

## 2. Phase ordering

```
Phase 0   Versioning ceremony           (5 min)
Phase 1   PRE-WORK: rotate + verify     (~30 min, mostly automation, 1 user step)
Phase 2   Rehearsal C4–C6               (variable — needs Anton interactions)
Phase 3   Phase D pre-launch cleanup    (~20 min)
Phase 4   Code review                   (~15 min, code-reviewer agent)
Phase 5   Brainstorm extensions         (~30 min, brainstorming skill)
Phase 6   Tag 0.1.1 + closeout doc      (~10 min)
```

Phases 1 and 2 can interleave — while waiting on Anton for the bot-token rotation or for him to post C5 messages, I can advance the next non-blocked task.

## 3. Items requiring Anton

Flag these clearly so Anton can do them in any order; everything else automated.

| # | Action | Required for |
|---|---|---|
| **A1** | `/revoke` in `@BotFather` chat → paste new token here | Phase 1 (bot token rotation) |
| **A2** | DM `@WarsawAIBrainBot` with `/gbrain-forget <archive-url>` (I'll give the URL) | Phase 2 — C4 |
| **A3** | Post 3 messages with `#kb` in `gbrain-staging` News & Signals topic | Phase 2 — C5 |
| **A4** | (Decision) Approve 0.1.1 release after code review & before tag | Phase 6 |

Everything else: I drive via Vercel REST + Telegram Bot API.

---

## Phase 0 — Versioning ceremony

### Task V1: Bump `app/package.json` to 0.1.0
- **File:** `projects/gbrain/app/package.json`
- **Edit:** `"version": "0.0.1"` → `"version": "0.1.0"`
- **Why:** Align with the `gbrain-v0.1.0` git tag and CHANGELOG `[0.1.0]` entry. The mismatch is a footgun for future tooling.

### Task V2: Add `[0.1.1] - Unreleased` section to CHANGELOG
- **File:** `projects/gbrain/CHANGELOG.md`
- **Insert** below `[0.1.0]`:
  ```md
  ## [0.1.1] - Unreleased — rehearsal complete

  ### Fixed
  - parse.ts: treat undefined `message_thread_id` as General topic when configured (b78f454, 630c557)
  - bot token rotation (env was stale vs. BotFather)

  ### Added
  - webhook error-capture wrapper (logs only, no stack in response body) — kept after Phase D refinement
  - rehearsal staging: 8 topics + env vars in `gbrain-staging`
  - Vercel ignored-build-step path filter — only deploy on `projects/gbrain/app/**` changes

  ### Removed
  - `/api/debug/env` diagnostic endpoint (was 0d5b993, reverted in this release)
  - `community/archive/_staging/` (rehearsal-only test data)
  - `ARCHIVE_NAMESPACE` env var (no longer needed post-staging)

  ### Security
  - Rotated `CRON_SECRET`, `TELEGRAM_WEBHOOK_SECRET`, `TELEGRAM_BOT_TOKEN`, Vercel API token (all exposed in session 3 chat history)
  ```

### Task V3: Add version policy line to README
- **File:** `projects/gbrain/README.md`
- **Add under "Phases":**
  > **Versioning:** SemVer. `0.1.x` = pre-launch rehearsal. `0.2.0` = first real-channel launch. `1.0.0` = Phase 1 success gates (spec §11). See `CHANGELOG.md`.

### Task V4: Commit
```bash
git add projects/gbrain/app/package.json projects/gbrain/CHANGELOG.md projects/gbrain/README.md
git commit -m "chore(gbrain): align package.json + CHANGELOG, document versioning model (0.1.1 wip)"
```

---

## Phase 1 — PRE-WORK: rotate + verify

### Task P1: Rotate `CRON_SECRET`
- Generate a 32-byte random string (don't echo).
- `PATCH https://api.vercel.com/v9/projects/<projId>/env/wi3Ota42aI8CpL4s` with `{ value: "<new>", target: ["production","preview","development"] }`.
- Trigger fresh build via `gitSource` (NOT `deploymentId` — see closeout §8 lesson 1).
- Wait for `readyState=READY`, then alias to production if not auto-aliased.
- Send Anton the new value via secure channel (he stores it; I forget after this turn).

### Task P2: Rotate `TELEGRAM_WEBHOOK_SECRET`
- Generate new 32-byte random string.
- `PATCH /v9/projects/<projId>/env/aUCqQ82wkHhBsikM`.
- Trigger fresh build.
- After build: `setWebhook` with `secret_token=<new>`.
- `getWebhookInfo` → assert `pending_update_count == 0`, no `last_error_*`.

### Task P3: Revoke leaked Vercel API token
- `GET /v3/user/tokens` → find token id matching the leaked prefix.
- `DELETE /v3/user/tokens/<id>`.
- **Anton:** issue a fresh API token in `vercel.com/account/tokens` and paste here so subsequent steps continue.

### Task P4: Bot token rotation — DEFERRED to Phase E
**Anton's call 2026-04-26:** the current bot token still works; rotation happens once when migrating to the real channel (Phase E), not now. Carries over to the Phase E session prompt.

**Side effect:** I still need the bot token *transiently* to call `setWebhook` with the new `TELEGRAM_WEBHOOK_SECRET` from P2. Anton pastes it during P2-completion; I use it for one Telegram API call and don't persist it.

### Task P5: Verify production alias at 630c557+
```bash
curl -sS -H "Authorization: Bearer $VERCEL_TOKEN" \
  "https://api.vercel.com/v6/deployments?projectId=prj_3aL8jM67XR1d0yz99f7wrrdpBECR&teamId=team_iEUo3hzS0aASHR0TEAB70Z8W&target=production&limit=3" \
  | jq '.deployments[] | {url, state: .readyState, sha: .meta.githubCommitSha[0:7], aliased: .aliasAssigned}'
```
- Confirm alias `warsawaicommunitygbrain.vercel.app` resolves to a deployment containing 630c557.
- If not: `POST /v2/aliases` with the right `deploymentId` and `alias`.

### Task P6: Final webhook health snapshot
- `getWebhookInfo` → record state for closeout doc.
- Send a `#skip` test message into `gbrain-staging` General — should be denied with reason logged (sanity check parser still handles General topic).

---

## Phase 2 — Rehearsal tests C4–C6

### Task C4: `/gbrain-forget` round-trip
- I send Anton a target archive URL (e.g., `community/archive/_staging/2026-04/2026-04-26-general-kb-quick-tip-pin-python-deps-with.md`).
- **Anton (A2):** DMs `@WarsawAIBrainBot` `/gbrain-forget <url>`.
- Verify: file removed from working tree (next `git pull`), tombstone created in `community/archive/_staging/_removed/2026-04-26.md` (hash-only entry).
- If failure: capture from webhook response body (the error-capture wrapper), diagnose, fix, re-test.

### Task C5: Daily digest with non-zero items
- **Anton (A3):** post 3 messages tagged `#kb` in `gbrain-staging` News & Signals topic. Wait until each gets the `🧠`/`👀` reaction (= ingest acked).
- I `GET /api/cron/daily-digest` with `Authorization: Bearer <new CRON_SECRET>`.
- Expected response: `{ok:true, itemCount:3, digestUrl:"..."}`.
- Verify: digest message appears in News & Signals topic; `community/archive/_staging/digests/2026-04-26.md` exists.
- Verify the news-log persistence fix: subsequent cron run within 24h returns `itemCount:0` (already-digested items not re-counted).

### Task C6a: AI down → graceful degrade
- Snapshot current `AI_GATEWAY_API_KEY` + `GEMINI_API_KEY`.
- `PATCH` both to bogus values (e.g., `bogus-test-2026-04-26`).
- Trigger fresh build (env propagation rule).
- `GET /api/cron/daily-digest` → expected: 200 with body `{ok:false, reason:"ai_unavailable", degraded:true}` or similar (whatever the current implementation produces — record it).
- Restore both keys + fresh build.
- Re-run cron → confirm normal operation.

### Task C6c: Kill switch ON
- `PATCH` env `GBRAIN_KILL_SWITCH=true`. Fresh build.
- `GET /api/cron/daily-digest` → expected: 200 with `{ok:true, suppressed:true}`, no LLM call (verify in Vercel logs).
- POST a `#kb` message → expected: webhook returns 200 but no archive commit, no Telegram reaction.
- Unset (or set to `false`). Fresh build. Confirm normal flow.

### Task C3: Cross-`#kb` with Jurek — keep deferred
- Stays in **0.1.x backlog** (needs Jurek in `gbrain-staging`). Mention in closeout.

---

## Phase 3 — Phase D pre-launch cleanup

### Task D1: Revert `/api/debug/env` endpoint
```bash
git revert 0d5b993 --no-edit
```
- Verify build still passes.

### Task D2: Decide error-capture wrapper (commit 543fa91)
- **Decision (default):** keep, but harden — log full error to console (Vercel logs already redact secrets); response body returns generic `{ok:false}`. Stack stays out of response.
- If the current code already returns the stack in body, edit `app/api/telegram/webhook/route.ts` to:
  ```ts
  } catch (err) {
    console.error("[gbrain.webhook] ingest failed:", err);
    return NextResponse.json({ok: false}, {status: 200});
  }
  ```

### Task D3: Remove `_staging` archive
```bash
git rm -rf community/archive/_staging
git commit -m "chore(gbrain): remove rehearsal staging archive"
```

### Task D4: Trim env, set ignoredBuildStep
- `DELETE /v9/projects/<projId>/env/<ARCHIVE_NAMESPACE id>`.
- Keep `NODE_OPTIONS=--disable-warning=DEP0169` (cosmetic, near-zero cost).
- `PATCH /v9/projects/<projId>` with `commandForIgnoringBuildStep`:
  ```sh
  if git diff HEAD^ HEAD --quiet -- projects/gbrain/app/; then exit 0; else exit 1; fi
  ```
  (`exit 0` = skip deploy; `exit 1` = run deploy.)
- Trigger one fresh build to confirm config sticks.

### Task D5: Push everything
```bash
git push origin main
```

---

## Phase 4 — Code review (clarification #3)

### Task R1: Run `code-reviewer` agent on session 2-3 changes
- Scope: commits `165b54d` (Phase 1 hardening), `8a30d30` (error-capture), `b78f454` + `630c557` (parse.ts General fix), Phase D edits from this session, plus `app/package.json` + CHANGELOG bumps.
- Severity gate: address CRITICAL + HIGH before tag. MEDIUM noted in 0.1.2 backlog.
- Specifically request review of: error handling in webhook + cron, secret hygiene (no values in logs/errors), correctness of General-topic detection, idempotency of digest cron, and env-var validation at startup.

### Task R2: Address findings
- Fix CRITICAL/HIGH inline.
- Each fix: own commit, conventional message.

---

## Phase 5 — Brainstorm extensions (clarification #4)

### Task B1: Invoke `superpowers:brainstorming` skill
- Topic: *"What else can we attach to / get out of the GBrain Telegram + repo + AI-Gateway + cron setup beyond the v0.1 archive + digest? Alternative uses? Adjacent products?"*
- Output: `docs/specs/2026-04-26-gbrain-extension-ideas.md` with prioritized ideas, each with: hypothesis · effort · cost-to-build · cost-to-run · risk · whether-it-extends-or-replaces-current-architecture.

### Task B2: Tag winners
- Mark top 1-2 ideas as candidates for the **0.3.0 / 1.x roadmap**.
- Anything explicitly out-of-scope-for-now → `docs/specs/gbrain-icebox.md` (one-liner each).

---

## Phase 6 — Tag 0.1.1 + session 4 closeout

### Task F1: Update CHANGELOG to release form
- `[0.1.1] - Unreleased` → `[0.1.1] - 2026-04-26`.
- Make sure Removed/Added/Fixed/Security match what actually happened (not what was planned).

### Task F2: Bump `app/package.json` to 0.1.1
- `"version": "0.1.0"` → `"version": "0.1.1"`.

### Task F3: Commit + tag + push
```bash
git add projects/gbrain/CHANGELOG.md projects/gbrain/app/package.json
git commit -m "release(gbrain): 0.1.1 — rehearsal complete + Phase D cleanup"
git tag -a gbrain-v0.1.1 -m "GBrain 0.1.1 — rehearsal complete + Phase D cleanup"
git push origin main gbrain-v0.1.1
```

### Task F4: Write session 4 closeout doc
- **File:** `docs/specs/2026-04-26-gbrain-session-4-closeout.md`
- Sections:
  1. What session 4 accomplished (PRE-WORK + C4-C6 results + Phase D + version bump + code review fixes)
  2. Brainstorm summary (link to extension-ideas doc)
  3. Code-review highlights (what was found, what was fixed, what's deferred)
  4. Open backlog: C3 (Jurek), any 0.1.2+ items, anything brainstorm flagged
  5. Lessons learned for session 5 (real-channel launch, separate chat)
  6. Next-chat prompt (Phase E)

### Task F5: Update `projects/gbrain/plan.md` with session 4 progress
- Mark Phase D items done.
- Note that Phase E (real-channel) opens in a separate chat from `2026-04-26-gbrain-session-4-closeout.md`.

---

## Rollback strategy per phase

| Phase | Failure mode | Rollback |
|---|---|---|
| 0 (versioning) | Wrong version number commits | `git revert <sha>`, redo |
| 1 (secrets) | Build doesn't pick up new secret | Re-trigger build via `gitSource`, never `deploymentId` |
| 1 (bot token) | Webhook 401s after rotation | Re-`setWebhook` from a known-good token; if BotFather rotation lost — Anton issues `/token` in BotFather |
| 2 (rehearsal) | Test exposes a bug | Add to 0.1.2 backlog; don't tag 0.1.1 with known broken behavior |
| 3 (cleanup) | `_staging` removal breaks something downstream | `git revert` the rm commit; investigate before redoing |
| 4 (review) | Reviewer finds CRITICAL | Don't tag 0.1.1; fix first |
| 5 (brainstorm) | n/a (read-only output) | — |
| 6 (release) | Bad commit tagged | `git tag -d gbrain-v0.1.1 && git push --delete origin gbrain-v0.1.1`; fix; retag |

---

## Self-review (writing-plans skill checklist)

1. **Spec coverage:** All 7 PRE-WORK + remaining tests + Phase D items from session 3 closeout §7 mapped to tasks above. Clarifications #1–#4 (automation, versioning, code review, brainstorm) all have phases. ✓
2. **Placeholders:** None — exact API endpoints, exact files, exact commands. ✓
3. **Type consistency:** Version model is consistent throughout (0.1.0 → 0.1.1 → 0.2.0 → 1.0.0). Task IDs match TaskCreate ids. ✓

---

*End of plan. Execution begins after Anton confirms.*
