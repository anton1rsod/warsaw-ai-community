# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*~

# Env / secrets
.env
.env.*
!.env.example
!.env.local.example
*.pem
*.key

# Node
node_modules/
dist/
build/
coverage/
.pnpm-store/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Large binaries (use Git LFS if needed)
*.mp4
*.mov
*.zip
*.tar.gz

# Local notes not meant for the repo
/scratch/
/private/

# Claude/agent caches (if any tooling runs locally)
.claude/local/

# Vercel CLI linkage (local-only; project linked at repo root for monorepo
# CLI deploys per Phase 10 Option C — `pnpm dlx vercel --prod` runs from here)
.vercel/
